<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">CryptoWallet.BlazorWasm</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
 
  
@*         <div class="nav-item px-3">
            <NavLink class="nav-link" href="TradingJournal">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span>   Journal
            </NavLink>
        </div> *@
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="DataGridTradingJournal">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> DataGrid Journal
            </NavLink>
        </div>  
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="AssetJournal">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Asset Journal
            </NavLink>
        </div>   
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="TradingChart">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Trade Option Chart
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
